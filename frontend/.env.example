# Copy to .env.local (or .env) for local development.

# Base URL of the Spring Boot API (includes the /api context path).
VITE_API_URL=http://localhost:8080/api

# Set to "true" to run the frontend on local mock data (offline dev).
# Leave "false" to use the real backend (default).
VITE_USE_MOCKS=false
